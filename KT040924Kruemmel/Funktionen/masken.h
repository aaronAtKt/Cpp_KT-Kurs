#ifndef MASKEN_H
#define MASKEN_H

// Deklaration der Funktionen für Bitmaskenoperationen
int applyMaskAdd(int number, int mask);
int applyMaskClear(int number, int mask);
int applyMaskCheck(int number, int mask);

#endif
