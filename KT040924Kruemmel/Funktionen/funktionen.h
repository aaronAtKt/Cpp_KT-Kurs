#ifndef FUNKTIONEN_H
#define FUNKTIONEN_H

// Deklaration der Funktion zum Setzen eines Bits
int setBit(int number, int position);

// Deklaration der Funktion zum Löschen eines Bits
int clearBit(int number, int position);

// Deklaration der Funktion zum Prüfen eines Bits
bool checkBit(int number, int position);

#endif
